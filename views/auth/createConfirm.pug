extends ../layout/index

block contenido
    div.py-10 
        h1.text-4xl.my-10.font-extrabold.text-center.text-cyan-300  Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold.text-sky-400= page 
    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-rosa' : 'bg-morado').py-2.px-5.text-white.uppercase
            = mensaje

        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase") Ya puedes Iniciar Sesión
