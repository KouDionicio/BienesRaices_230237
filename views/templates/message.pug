extends ../layout/index

block contenido
    input(type="hidden" name="_csrf" value=csrfToken)
    .py-10
        h1.text-center.text-4xl.my-10.font-extrabold.text-cyan-300 Bienes
            span.font-normal.text-black Raíces
        h2.text-center.font-extrabold.text-2xl= page
        center
            span.text-xl.font-bold.text-center.my-10.text-red-600 Hemos enviado un correo a: 
            span.text-xl.font-bold= msg
            span.text-xl.font-bold.text-center.my-10.text-red-600 , para la confirmación de la cuenta
        form(method='POST' action='/auth/createAccount' noValidate).space-y-5.px-6
            input(type="hidden" name="_csrf" value=csrfToken)